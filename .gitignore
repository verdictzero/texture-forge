# playwright is a dev dependency of tools/smoke-test.mjs, not of the app —
# the app itself has no build step and no dependencies at all
node_modules/
package.json
package-lock.json

# screenshots the smoke test writes — now outside the publish root, since a
# capture can contain glyphs from a face this repo may not redistribute
.shots/
# where they used to go; still ignored so an old checkout does not publish them
texture-forge/tools/shots/
